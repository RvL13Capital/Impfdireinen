# Streamlit theme & server config (used by `streamlit run` and Streamlit Cloud).
[theme]
base = "dark"
primaryColor = "#26a69a"
backgroundColor = "#0e1117"
secondaryBackgroundColor = "#1a1f2b"
font = "monospace"

[server]
headless = true
